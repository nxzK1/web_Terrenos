{
  "name": "terrenos-sebastian-valdes",
  "version": "1.0.0",
  "description": "Landing page profesional — Venta de terrenos en la playa, 5ta Region de Chile.",
  "private": true,
  "scripts": {
    "dev": "tailwindcss -i ./src/input.css -o ./dist/styles.css --watch",
    "build": "tailwindcss -i ./src/input.css -o ./dist/styles.css --minify",
    "clean": "rm -rf dist && mkdir dist"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.4",
    "autoprefixer": "^10.4.19",
    "postcss": "^8.4.38"
  }
}
